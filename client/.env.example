# Client Environment Variables

# No specific environment variables are currently required for the client.
# You can add API URLs or other config here if needed in the future.
# VITE_API_URL=http://localhost:3000
